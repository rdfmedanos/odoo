    <template id="payment_page" name="Mercado Pago Card Payment">
        <t t-call="website.layout">
            <style>
                /* Modern spinner styling */
                .mp-spinner-container {
                    display: flex;
                    flex-direction: column;
                    align-items: center;
                    justify-content: center;
                    padding: 50px 20px;
                }
                .mp-spinner {
                    width: 44px;
                    height: 44px;
                    border: 4px solid #f1f5f9;
                    border-top-color: #3b82f6;
                    border-radius: 50%;
                    animation: mp-spin 0.8s cubic-bezier(0.4, 0, 0.2, 1) infinite;
                }
                @keyframes mp-spin {
                    to { transform: rotate(360deg); }
                }
                .mp-loading-text {
                    margin-top: 20px;
                    color: #64748b;
                    font-size: 15px;
                    font-weight: 500;
                }

                /* Modern inline error alert styling */
                #mp-error {
                    padding: 16px 20px;
                    border-radius: 12px;
                    background-color: #fef2f2;
                    border: 1px solid #fca5a5;
                    color: #991b1b;
                    font-size: 14px;
                    font-weight: 500;
                    margin-top: 15px;
                    box-shadow: 0 4px 6px -1px rgba(239, 68, 68, 0.05);
                }

                /* Modal overlay styling with backdrop blur and dark semi-transparent bg */
                .mp-modal-overlay {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100vw;
                    height: 100vh;
                    background-color: rgba(15, 23, 42, 0.5); /* Slate-900 with transparency */
                    backdrop-filter: blur(8px);
                    -webkit-backdrop-filter: blur(8px);
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    z-index: 10000;
                    opacity: 0;
                    pointer-events: none;
                    transition: opacity 0.25s ease;
                }

                .mp-modal-overlay.active {
                    opacity: 1;
                    pointer-events: auto;
                }

                /* Modal box styling */
                .mp-modal-box {
                    background: #ffffff;
                    border-radius: 24px;
                    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15);
                    width: 90%;
                    max-width: 440px;
                    padding: 36px 30px;
                    text-align: center;
                    position: relative;
                    transform: scale(0.92);
                    transition: transform 0.25s cubic-bezier(0.34, 1.56, 0.64, 1);
                    border: 1px solid rgba(226, 232, 240, 0.8);
                }

                .mp-modal-overlay.active .mp-modal-box {
                    transform: scale(1);
                }

                /* Modal header/icon style */
                .mp-modal-icon-container {
                    width: 64px;
                    height: 64px;
                    background-color: #fee2e2; /* Light red/rose */
                    border-radius: 50%;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    margin: 0 auto 24px;
                    color: #ef4444; /* Bright red */
                    box-shadow: 0 0 0 8px #fef2f2;
                }
                .mp-modal-icon {
                    width: 32px;
                    height: 32px;
                }

                /* Title and description styling */
                .mp-modal-title {
                    font-size: 22px;
                    font-weight: 700;
                    color: #1e293b; /* Slate-800 */
                    margin-bottom: 12px;
                    font-family: inherit;
                }

                .mp-modal-message {
                    font-size: 15px;
                    color: #64748b; /* Slate-500 */
                    line-height: 1.6;
                    margin-bottom: 28px;
                }

                /* Close/Accept Button */
                .mp-modal-btn {
                    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
                    color: #ffffff !important;
                    border: none;
                    padding: 14px 30px;
                    font-size: 16px;
                    font-weight: 600;
                    border-radius: 12px;
                    cursor: pointer;
                    width: 100%;
                    box-shadow: 0 4px 6px -1px rgba(239, 68, 68, 0.2), 0 2px 4px -1px rgba(239, 68, 68, 0.1);
                    transition: all 0.2s ease;
                }

                .mp-modal-btn:hover {
                    background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
                    transform: translateY(-2px);
                    box-shadow: 0 10px 15px -3px rgba(239, 68, 68, 0.3), 0 4px 6px -2px rgba(239, 68, 68, 0.15);
                }

                .mp-modal-btn:focus {
                    outline: none;
                    box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.4);
                }

                .mp-modal-btn:active {
                    transform: translateY(0);
                }
            </style>

            <div id="wrap" class="container py-4">
                <div class="row justify-content-center">
                    <div class="col-12 col-lg-6">
                        <div class="card">
                            <div class="card-body">
                                <h4 class="card-title mb-3">Pago con tarjeta</h4>
                                <p class="text-muted mb-3">
                                    Monto: <strong>$<t t-esc="'%.2f' % amount"/></strong>
                                </p>

                                <div id="cardPaymentBrick_container" style="min-height: 350px;">
                                    <div id="mp-loading" class="mp-spinner-container">
                                        <div class="mp-spinner"></div>
                                        <div class="mp-loading-text">Cargando formulario de pago...</div>
                                    </div>
                                    <div id="mp-error" style="display: none; text-align: center;"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Custom Error Modal -->
            <div id="mp-modal-overlay" class="mp-modal-overlay">
                <div class="mp-modal-box">
                    <div class="mp-modal-icon-container">
                        <svg class="mp-modal-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z" />
                        </svg>
                    </div>
                    <h3 class="mp-modal-title">No se pudo procesar el pago</h3>
                    <p id="mp-modal-message" class="mp-modal-message"></p>
                    <button type="button" id="mp-modal-close-btn" class="mp-modal-btn">Entendido</button>
                </div>
            </div>
        </t>

        <t t-set="l10n_ar_mp_pk" t-value="public_key"/>
        <t t-set="l10n_ar_mp_amount" t-value="amount"/>
        <t t-set="l10n_ar_mp_ref" t-value="reference"/>
        <t t-set="l10n_ar_mp_email" t-value="partner_email"/>

        <script>
            window.l10nArMpConfig = {
                publicKey: "<t t-esc="l10n_ar_mp_pk"/>",
                amount: <t t-esc="l10n_ar_mp_amount"/>,
                reference: "<t t-esc="l10n_ar_mp_ref"/>",
                email: "<t t-esc="l10n_ar_mp_email"/>",
            };
        </script>

        <script src="https://sdk.mercadopago.com/js/v2"></script>
        <script><![CDATA[
            (function() {
                console.log('[MP] init', window.l10nArMpConfig);
                var cfg = window.l10nArMpConfig;

                function showErrorModal(message) {
                    var overlay = document.getElementById('mp-modal-overlay');
                    var msgEl = document.getElementById('mp-modal-message');
                    if (overlay && msgEl) {
                        msgEl.textContent = message;
                        overlay.classList.add('active');
                    } else {
                        alert(message);
                    }
                }

                function hideErrorModal() {
                    var overlay = document.getElementById('mp-modal-overlay');
                    if (overlay) {
                        overlay.classList.remove('active');
                    }
                }

                var closeBtn = document.getElementById('mp-modal-close-btn');
                if (closeBtn) {
                    closeBtn.addEventListener('click', hideErrorModal);
                }

                var overlay = document.getElementById('mp-modal-overlay');
                if (overlay) {
                    overlay.addEventListener('click', function(e) {
                        if (e.target === overlay) {
                            hideErrorModal();
                        }
                    });
                }

                if (!cfg.publicKey) {
                    document.getElementById('mp-loading').style.display = 'none';
                    var err = document.getElementById('mp-error');
                    err.textContent = 'Error: Mercado Pago no esta configurado (falta Public Key).';
                    err.style.display = 'block';
                    return;
                }
                if (!cfg.amount || cfg.amount <= 0) {
                    document.getElementById('mp-loading').style.display = 'none';
                    var err = document.getElementById('mp-error');
                    err.textContent = 'Error: El monto de la transaccion es invalido.';
                    err.style.display = 'block';
                    return;
                }

                var mp = new MercadoPago(cfg.publicKey, { locale: 'es-AR' });
                var bricksBuilder = mp.bricks();

                var settings = {
                    initialization: { amount: Number(cfg.amount) },
                    callbacks: {
                        onReady: function() {
                            console.log('[MP] brick ready');
                            document.getElementById('mp-loading').style.display = 'none';
                        },
                        onSubmit: function(formData, additionalData) {
                            console.log('[MP] onSubmit', formData, additionalData);
                            return new Promise(function(resolve, reject) {
                                var submitData = {
                                    token: formData.token,
                                    payment_method_id: formData.payment_method_id,
                                    payment_type_id: formData.payment_type_id || (additionalData && additionalData.paymentTypeId) || 'credit_card',
                                    installments: Number(formData.installments) || 1,
                                    external_reference: cfg.reference,
                                    payer: {
                                        email: (formData.payer && formData.payer.email) || cfg.email,
                                        identification: (formData.payer && formData.payer.identification) || {},
                                    },
                                };
                                console.log('[MP] sending', submitData);
                                fetch('/payment/mercado_pago/process_order', {
                                    method: 'POST',
                                    headers: { 'Content-Type': 'application/json' },
                                    body: JSON.stringify(submitData),
                                })
                                .then(function(res) { return res.json(); })
                                .then(function(data) {
                                    console.log('[MP] response', data);
                                    if (data.success) {
                                        window.location.href = data.redirect_url;
                                    } else {
                                        showErrorModal(data.error || 'No se pudo procesar el pago');
                                        reject();
                                    }
                                    resolve();
                                })
                                .catch(function(err) {
                                    console.error('[MP] fetch error', err);
                                    showErrorModal('Error de conexion al procesar el pago');
                                    reject();
                                });
                            });
                        },
                        onError: function(error) {
                            console.error('[MP] brick error', error);
                            document.getElementById('mp-loading').style.display = 'none';
                            var err = document.getElementById('mp-error');
                            err.textContent = 'Error al cargar el formulario de pago. Recargue la pagina e intente nuevamente.';
                            err.style.display = 'block';
                        },
                    },
                };
                bricksBuilder.create('cardPayment', 'cardPaymentBrick_container', settings)
                .then(function() {
                    console.log('[MP] brick created');
                })
                .catch(function(error) {
                    console.error('[MP] create error', error);
                    document.getElementById('mp-loading').style.display = 'none';
                    var err = document.getElementById('mp-error');
                    err.textContent = 'Error al cargar formulario: ' + (error.message || JSON.stringify(error));
                    err.style.display = 'block';
                });
            })();
        ]]></script>
    </template>